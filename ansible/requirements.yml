---
# Pinned: an unpinned major bump can change docker_compose_v2 behaviour with no
# change in this repo.
collections:
  - name: community.general
    version: "13.4.0"
  - name: community.docker
    version: "5.3.0"
  - name: ansible.posix
    version: "2.2.2"

roles:
  # The standard Docker installation role. Used only on releases Docker's apt
  # repository actually serves; see roles/docker/tasks/main.yml.
  - name: geerlingguy.docker
    version: 7.4.7
